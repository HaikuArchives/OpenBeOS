rule LinkSharedOSLibs 
{ 
        # LinkSharedOSLibs <name> : <libs> ; 
        
        for i in $(>) 
        { 
				LINKFLAGS on $(<) += $(i) ;
#                LINKLIBS on $(<) += $(i) ; 
        } 
}

rule AddDebugInfo {
#	C++FLAGS on $(<) += "-g -O0" ;
	C++FLAGS += "-g -O0" ;
}